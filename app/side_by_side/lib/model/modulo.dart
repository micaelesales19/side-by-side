class Modulos {
  int id;
  String title, subtitle, descricao, parte1, parte2, parte3, capa;

  Modulos({
    required this.id,
    required this.title,
    required this.subtitle,
    required this.descricao,
    required this.parte1,
    required this.parte2,
    required this.parte3,
    required this.capa,
  });
}
